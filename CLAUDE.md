# OpenPocketCine Claude Code guide

## Canonical project guidance

```text
@import AGENTS.md
@import docs/ARCHITECTURE.md
```

`AGENTS.md` is the single source of truth for architecture, coding standards, repository hygiene,
and verification. Keep shared guidance there instead of duplicating it in this client-specific file.

**UI / chrome tweaks:** build-run on a physical iPhone and/or `just android-install` after the
change — compile-only is not done. See `AGENTS.md` → Verification.
